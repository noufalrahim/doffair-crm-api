Each vendor will have one or more service types (grroming, vet, etc)

Each serbice type will have one or more loocations ---> franchise model



Each location will have one or more services (bathing, nail cutting, etc)

Each service will have one pricing --> 100
    If your pet is large then +30
    If its mendim then its +50
    and so on



Service type will have 2 types - At home , in center
if at home then what radius or zipcode it supports 



There are two modes
1. Booking  ---> USer paid -> auto confirmed 
    User paid --> Pending with vendor for approval ---> VEndor approves -> Confirm
            --> vendor rejects --> refund

2. Lead Generation
   All the amenitis and information about the vendor on the app. 
   Initially we will hide the mobile number part. 
   once suer clicks on reveal mobile, it will display the mobile number and creates a lead and share the users mobile number to vendor so that vendor can connect with the user.

   Also we will keep a threshod on it. in a day user can select max 5 numbers.
